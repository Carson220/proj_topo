1. test_X & switch_X文件（以时间片序号命名）
第一行是卫星节点数量num
以下每一行有三个数字，分别是 node1 node2 dist

2. ctrl文件
第一行是卫星节点数量num
第二行有num个数字，分别是 每个交换机对应的控制器所在节点序号

3. db文件
第一行是数据库节点数量db_num 各个时间片的控制器到最近的数据库的距离之和dist_sum
第二行是数据库所在节点序号

4. db_X文件（以时间片序号命名）
每两行分别是 
	数据库所在节点序号 
	与其相连的控制器所在节点序号

5. c2d_X文件（以时间片序号命名）
每两行分别是
	数据库所在节点序号 控制器所在节点序号 路由表项sw+outport
	控制器所在节点序号 数据库所在节点序号 路由表项sw+outport
注：用于记录控制器到主备数据库的默认路由

6. d2d_X文件（以时间片序号命名）
每db_num行分别是
	源数据库所在节点序号db_src
	源数据库所在节点序号db_src 目的数据库所在节点序号db_dst 路由表项sw+outport

7. ctrl_X（以控制器所在节点序号命名）
每一行分别是相应时间片按距离递增排列的数据库所在节点序号

8. del_link_X（以时间片序号命名）
每一行有两个数字，分别是第(X+1)个时间片将会删除的链路交换机所在节点序号

注：文件56均不包含 同一交换机节点上不同设备之间的路由表项，
因此，需要单独对每个交换机上的不同设备设置相应的端口转发策略。