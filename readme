1. test_X & switch_X文件（以时间片序号命名）
第一行是卫星节点数量num
以下每一行有三个数字，分别是 node1 node2 dist

2. ctrl文件
第一行是卫星节点数量num
第二行有num个数字，分别是 每个交换机对应的控制器所在节点序号

3. db文件
第一行是数据库节点数量db_num 各个时间片的控制器到最近的数据库的距离之和dist_sum
第二行是数据库所在节点序号

4. db_X文件（以时间片序号命名）
每两行分别是 
	数据库所在节点序号 
	与其相连的控制器所在节点序号

5. s2s_X文件（以时间片序号命名）
每num行分别是 
	源交换机所在节点序号sw_src
	源交换机所在节点序号sw_src 目的交换机所在节点序号sw_dst 路由表项sw+outport

6. c2s_X文件（以时间片序号命名）
每两行分别是
	控制器（包括主备控制器）所在节点序号 受其控制的交换机所在节点序号 路由表项sw+outport
	交换机所在节点序号 控制器所在节点序号 路由表项sw+outport

7. c2d_X文件（以时间片序号命名）
每两行分别是
	数据库所在节点序号 与其相连的控制器所在节点序号 路由表项sw+outport
	控制器所在节点序号 数据库所在节点序号 路由表项sw+outport

8. d2d_X文件（以时间片序号命名）
每db_num行分别是
	源数据库所在节点序号db_src
	源数据库所在节点序号db_src 目的数据库所在节点序号db_dst 路由表项sw+outport

9. slot_X_ctrl_Y（以时间片序号和控制器所在节点序号命名）
第一行是时间片序号
第二行是控制器所在节点序号+1(IP)
第三行是控制器连接的数据库所在节点序号+1(IP)

10. del_link_X（以时间片序号命名）
每一行有两个数字，分别是第(X+1)个时间片将会删除的链路交换机所在节点序号

注：5678四个文件均不包含 同一交换机节点上不同设备之间的路由表项，
因此，需要单独对每个交换机上的不同设备设置相应的端口转发策略。